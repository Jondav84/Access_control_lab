# Core Directory Permissions Transcript
# Directory: ProjectX_hardened_live/core/
# Goal: core_team owns this namespace; other dev teams + reviewers may read/traverse; no cross-team writes.

## 1) Baseline (owner/group + chmod)_

# Command(s) used:
#   sudo chown project_lead:core_team ProjectX_hardened_live/core/
#   sudo chmod 770 ProjectX_hardened_live/core/

# Result (ls -ld):
#   drwxrwx--- 2 project_lead core_team 4096 Feb  1 04:37 ProjectX_hardened_live/core/

## 2) Access ACLs (incremental trust decision)

# api_team:
# Purpose: allow read/traverse across core code for integration and shared interfaces
# Scope: read + traverse (no write)
# Command:
#   sudo setfacl -m g:api_team:r-x ProjectX_hardened_live/core/

# ui_team:
# Purpose: allow read/traverse across core code for UI integration and dependency inspection
# Scope: read + traverse (no write)
# Command: 
#   sudo setfacl -m g:ui_team:r-x ProjectX_hardened_live/core/

# reviewers:
# Purpose: allow read/traverse across core code for audit and review
# Scope: read + traverse (no write)
# Command:
#   sudo setfacl -m g:reviewers:r-x ProjectX_hardened_live/core/

## 3) Default ACLs (inheritance template)

# Purpose: ensure newly created files and subdirectories in core/ inherit correct visibility
# Notes:
#   - core_team retains full ownership and write access.
#   - api_team, ui_team, and reviewers inherit read/traverse only.
#   - testers are excluded (no source visibility).

# Command(s) used:
#   sudo setfacl -d -m g:api_team:r-x ProjectX_hardened_live/core/
#   sudo setfacl -d -m g:ui_team:r-x ProjectX_hardened_live/core/
#   sudo setfacl -d -m g:reviewers:r-x ProjectX_hardened_live/core/

# Final permissions structure (getfacl -p ProjectX_hardened_live/core):
# file: ProjectX_hardened_live/core
# owner: project_lead
# group: core_team
user::rwx
group::rwx
group:api_team:r-x
group:ui_team:r-x
group:reviewers:r-x
mask::rwx
other::---
default:user::rwx
default:group::rwx
default:group:api_team:r-x
default:group:ui_team:r-x
default:group:reviewers:r-x
default:mask::rwx
default:other::---

